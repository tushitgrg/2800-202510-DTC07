export const ClientUrl = process.env.NEXT_PUBLIC_CLIENT_URL; //client url fetched from env, so can be switched between different evironments
export const ServerUrl = process.env.NEXT_PUBLIC_SERVER_URL; //server url fetched from env, so can be switched between different evironments
